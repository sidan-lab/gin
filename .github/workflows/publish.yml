name: publish
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+" # Push events to matching *, i.e. 1.0, 20.15.10

jobs:
  publish:
    strategy:
      fail-fast: false
      matrix:
        python-version: [3.11]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: "recursive" # This will fetch all submodules recursively
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y swig libssl-dev
      - name: Install poetry
        run: pipx install poetry
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Build submodule
        run: |
          chmod +x ./src/sidan_gin/python_signing_module/build.sh
          cd ./src/sidan_gin/python_signing_module && ./build.sh
      - name: Install dependencies
        run: |
          poetry install
      - name: Lint with flake8
        run: |
          poetry run flake8 src --ignore E501 --exclude src/sidan_gin/python_signing_module
      - name: Run unit tests
        run: |
          poetry run pytest --doctest-modules --ignore=examples --cov=sidan_gin --cov-config=.coveragerc --cov-report=xml
      - name: poetry-check
        run: |
          poetry check
      - name: poetry-publish
        run: |
          poetry publish --build --username __token__ --password ${{ secrets.PYPI_TOKEN }}
  github-releases-to-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Github Releases To Discord
        uses: SethCohen/github-releases-to-discord@v1.16.2
        with:
          webhook_url: ${{ secrets.WEBHOOK_URL }}
          color: "2105893"
          username: "Gin"
          avatar_url: "https://raw.githubusercontent.com/sidan-lab/brand_assets/0a8b4d17610e85bd6459d04ea99d0d0e9a96ff08/sidan_s_square.png"
          content: "||@everyone||"
          footer_title: "Changelog"
          footer_icon_url: "https://raw.githubusercontent.com/sidan-lab/brand_assets/0a8b4d17610e85bd6459d04ea99d0d0e9a96ff08/sidan_s_square.png"
          footer_timestamp: true
          max_description: "4096"
          reduce_headings: true
# name: publish
# on:
#   push:
#     tags:
#       - "v[0-9]+.[0-9]+.[0-9]+" # Push events to matching versions

# jobs:
#   build_wheels:
#     name: Build wheels on ${{ matrix.os }}
#     runs-on: ${{ matrix.os }}
#     strategy:
#       matrix:
#         os: [ubuntu-latest, windows-latest, macos-latest]
#         python-version: ["3.8", "3.9", "3.10", "3.11"]

#     steps:
#       - uses: actions/checkout@v4
#         with:
#           submodules: recursive

#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v4
#         with:
#           python-version: ${{ matrix.python-version }}

#       - name: Install Rust
#         uses: dtolnay/rust-toolchain@stable

#       - name: Install dependencies (Linux)
#         if: runner.os == 'Linux'
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y swig libssl-dev

#       - name: Install dependencies (macOS)
#         if: runner.os == 'macOS'
#         run: |
#           brew install swig openssl

#       - name: Install dependencies (Windows)
#         if: runner.os == 'Windows'
#         run: |
#           choco install swig
#           choco install openssl

#       - name: Build Python signing module
#         shell: bash
#         run: |
#           cd src/sidan_gin/python_signing_module
#           if [ "${{ runner.os }}" = "Windows" ]; then
#             # Windows-specific build steps
#             cargo build
#             mkdir -p src/_binary/windows/amd64
#             cp target/debug/signer.lib src/libsigner.a
#             HEADER_PATH=$(find target/cxxbridge -name "lib.rs.h" -type f)
#             cp "$HEADER_PATH" src/lib.rs.h
#           else
#             # Use the regular build script for Linux and macOS
#             chmod +x build.sh
#             ./build.sh
#           fi

#       - name: Install cibuildwheel
#         run: |
#           python -m pip install cibuildwheel

#       - name: Build wheels
#         run: |
#           python -m cibuildwheel --output-dir wheelhouse
#         env:
#           CIBW_SKIP: "pp* cp27-* cp35-* cp36-* cp37-*"
#           CIBW_BEFORE_BUILD: |
#             python -m pip install -U setuptools wheel

#       - name: Upload wheels
#         uses: actions/upload-artifact@v3
#         with:
#           path: ./wheelhouse/*.whl

#   publish:
#     needs: build_wheels
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'
#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install twine
#       - name: Download all wheels
#         uses: actions/download-artifact@v3
#         with:
#           path: dist/
#       - name: Publish to PyPI
#         env:
#           TWINE_USERNAME: __token__
#           TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
#         run: |
#           twine upload dist/*/*.whl

#   github-releases-to-discord:
#     needs: publish
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#       - name: Github Releases To Discord
#         uses: SethCohen/github-releases-to-discord@v1.16.2
#         with:
#           webhook_url: ${{ secrets.WEBHOOK_URL }}
#           color: "2105893"
#           username: "Gin"
#           avatar_url: "https://raw.githubusercontent.com/sidan-lab/brand_assets/0a8b4d17610e85bd6459d04ea99d0d0e9a96ff08/sidan_s_square.png"
#           content: "||@everyone||"
#           footer_title: "Changelog"
#           footer_icon_url: "https://raw.githubusercontent.com/sidan-lab/brand_assets/0a8b4d17610e85bd6459d04ea99d0d0e9a96ff08/sidan_s_square.png"
#           footer_timestamp: true
